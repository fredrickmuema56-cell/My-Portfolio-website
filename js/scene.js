(async()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const mobile=matchMedia('(max-width: 640px)').matches;const mount=document.getElementById('three-bg');if(!mount||reduce||mobile)return;try{const THREE=await import('https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js');const canvas=document.createElement('canvas');canvas.className='three-canvas';mount.appendChild(canvas);const renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(45,innerWidth/innerHeight,.1,500);camera.position.z=15;const group=new THREE.Group();scene.add(group);const cyan=new THREE.MeshStandardMaterial({color:0x00c9d8,emissive:0x00aebc,emissiveIntensity:1.7,metalness:.7,roughness:.2});const gold=new THREE.MeshStandardMaterial({color:0xe4a84b,emissive:0x8f5b12,emissiveIntensity:.6,metalness:.8,roughness:.22});const geo=new THREE.IcosahedronGeometry(1.25,1);const core=new THREE.Mesh(geo,cyan);group.add(core);const ring=new THREE.Mesh(new THREE.TorusGeometry(2.1,.035,12,120),new THREE.MeshBasicMaterial({color:0x00c9d8,transparent:true,opacity:.7}));ring.rotation.x=1.1;group.add(ring);const ring2=new THREE.Mesh(new THREE.TorusGeometry(3,.025,12,120),new THREE.MeshBasicMaterial({color:0xe4a84b,transparent:true,opacity:.45}));ring2.rotation.x=.6;ring2.rotation.z=.4;group.add(ring2);const pts=new Float32Array((reduce?700:1500)*3);for(let i=0;i<pts.length;i+=3){const r=18+Math.random()*50,a=Math.random()*Math.PI*2;pts[i]=Math.cos(a)*r;pts[i+1]=(Math.random()-.5)*45;pts[i+2]=Math.sin(a)*r}const sg=new THREE.BufferGeometry();sg.setAttribute('position',new THREE.BufferAttribute(pts,3));scene.add(new THREE.Points(sg,new THREE.PointsMaterial({color:0x00c9d8,size:.045,transparent:true,opacity:.5})));scene.add(new THREE.AmbientLight(0xffffff,.8));const l=new THREE.PointLight(0x00c9d8,3,100);l.position.set(3,4,8);scene.add(l);const l2=new THREE.PointLight(0xe4a84b,2,80);l2.position.set(-5,-3,3);scene.add(l2);let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5);my=(e.clientY/innerHeight-.5)});addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});const clock=new THREE.Clock();function tick(){const t=clock.getElapsedTime();group.rotation.y=t*.08+mx*.35;group.rotation.x=-my*.15;core.rotation.x=t*.2;core.rotation.z=t*.12;ring.rotation.z=t*.25;ring2.rotation.y=-t*.18;camera.position.x+=(mx*1.3-camera.position.x)*.03;camera.position.y+=(-my*.8-camera.position.y)*.03;camera.lookAt(0,0,0);renderer.render(scene,camera);requestAnimationFrame(tick)}tick()}catch(e){console.warn('3D enhancement unavailable',e)}})();
